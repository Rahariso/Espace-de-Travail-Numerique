Installation XAMPP
trouver   C:\xampp\php   puis ajouter le dans Variables d’environnement, Variables système 
vérifier la version php -v dans cmd
Installation Composer,https://getcomposer.org/
vérifier la version composer -v dans cmd
Avant Installation laravel,Tu dois ✔ Activer l’extension ZIP dans PHP et ✔ Installer Git, trouver C:\xampp\php\php.ini ou C:\xampp\php\php puis ouvrer et chercher ;extension=zip en supprimant ;
vérifier php -m dans cmd il dois avoir zip, 
Installation GIT, voici lien, https://git-scm.com/ puis vérifier la version git --version dans cmd
Installation Laravel : creer d'abord un fichier puis ouvrer powershell, et lancer composer create-project laravel/laravel backend
